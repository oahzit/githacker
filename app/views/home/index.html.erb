<% if user_signed_in? %>
	<% content_for :nav do %>
	  <%= render 'layouts/nav/dashboard' %>
	<% end %>

	<h3>Home</h3>
	<% @users.each do |user| %>
	  <p>User: <%=link_to user.name, user %></p>
	<% end %>

<% else %>
	<% content_for :nav do %>
	  <!-- render 'layouts/nav/index' -->
	<% end %>
<% end %>
